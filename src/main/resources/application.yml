server:
  port: 8081
  tomcat:
    basedir: ./
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    session:
      timeout: 60m

spring:
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  application:
    name: inspector
  freemarker:
    suffix: .ftl
    cache: false
  datasource:
    url: jdbc:mysql://118.67.131.35:3306/finalproject?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: inspector               # Replace with your database username
    password: "NewPass1@!"                          # Database password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true #To beautify or pretty print the SQL
        show_sql: true #show sql
# ... (any other configurations)
